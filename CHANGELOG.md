## [1.5.2] - 2026-09-04

### Fixed
- 画像付きの投稿が表示されなかった。v1.4.0 で subtype 付きのメッセージを一律スキップするようにした際、画像付き投稿に必ず付く `file_share` も巻き込んで落としていた。`file_share` とスレッドの「チャンネルにも投稿」（`thread_broadcast`）は通常投稿として扱う
- テキストなしで画像だけを投稿すると、画像が届く前にキューから捨てられて表示されなかった
- 画像URLの取得に失敗したとき、別の候補（サムネイル等）を順に試すようにした

### Changed
- 表示ウィンドウの幅と高さを保存し、次回起動時に復元するようにした
- ユーザー情報のキャッシュを名前とアイコンの4項目に絞り、メモリとディスクの使用量を大幅に減らした（`users.json` は約1.9MB → 約220KB）。古い形式のファイルもそのまま読める
- 表示ウィンドウが1秒ごとにメッセージ件数ぶんの設定再読込をしていたのをやめ、設定変更時にだけ反映するようにした。絵文字変換の再計算も抑えた
- 設定ファイルの保存を一時ファイル経由のアトミック書き込みにし、書き込み途中でアプリが落ちてもトークンを含む設定が壊れないようにした
- 起動通知を監視チャンネルへ並列に投稿するようにした（チャンネル数ぶん待たされなくなる）

---

## [1.5.1] - 2026-09-01

### Added
- 表示ウィンドウのヘッダーにアプリのバージョンを表示するようにした

### Fixed
- OSのスリープから復帰したあと、接続が切れているのに気づけず「接続中」の表示のままメッセージが届かなくなることがあった。30秒ごとにPingを送るようにし、送信の失敗・90秒の受信途絶・スリープによる時計の飛びのいずれかで接続を張り直す

### Changed
- 表示ウィンドウのデフォルトの横幅を 500px から 440px に狭めた（既にウィンドウを動かしている場合は保存された値が優先される）

---

## [1.5.0] - 2026-09-01

### Added
- 起動時と監視終了時に、監視チャンネルへ通知を投稿するようにした（`chat:write` スコープが必要）。`OSユーザー名@ホスト名` を含むので、誰がどのマシンで起動しているかが分かる。接続が2本以上なら本数も載せるため、多重起動の状況もチャンネルのログから追える
- 受信経路の死活監視を追加した。起動通知が Events API 経由で戻ってくるかを確認し、戻ってこなければ Event Subscriptions が無効になっている可能性を警告する。`chat.postMessage` と Events API は別経路なので、両方通して初めて疎通を確認できる

### Fixed
- Socket Mode の受信ループが Slack Web API の応答を待つ間、後続イベントの ACK と Ping への Pong を返せなくなっていた問題を修正。イベント処理をワーカータスクに分離し、受信ループは ACK と Pong に専念させた。Slack は配信失敗が続くと Event Subscriptions を自動で無効化するため、メッセージの取りこぼしにつながっていた
- ヘルスチェックの `auth.test` も受信ループから分離した
- ユーザー情報と親メッセージの取得で毎回 HTTP クライアントを作り直していたのを、共有クライアントに変更（コネクションプールが効くようになった）

### Changed
- 接続の生死と関係ない警告を `socket-mode-warning` イベントに分離した。これまではヘルスチェックの一時的な失敗でも、接続が生きているのにUIが切断表示になっていた

---

## [1.4.0] - 2026-09-01

### Added
- 同一トークンでのWaigaya多重起動を検知して警告を表示するようにした。Socket Mode の `hello` に含まれる接続数を見て、2以上ならメッセージが各クライアントに分散している可能性を control ウィンドウに知らせる

### Changed
- 起動時に見つけたアップデートを同意を求めずに適用し、自動で再起動するようにした。適用中は control ウィンドウにダウンロード進捗を表示する。失敗しても起動は止めず、バナーと再試行ボタンを出す
  - v1.3.4 以前のクライアントには従来の「更新する」ボタンが出るため、このリリースだけは手動での適用が必要

---

## [1.3.4] - 2026-05-22

### Added
- Socket Modeの再接続・切断通知と画像の非同期取得を実装

### Fixed
- Socket Modeのタスク重複実行とAPI呼び出しタイムアウトを修正
- tauri-api.ts リスナーの Promise 完了前アンマウントによるメモリリークを修正
- Socket Mode 安定化のレビュー指摘点（4件）を修正
- SocketRetryOutcome を impl ブロック外に移してビルドエラーを解消

---

## [1.3.3] - 2026-05-22

### Fixed
- アプリ終了時にSocket Mode接続を適切にクローズするよう修正

---

## [1.3.2] - 2026-05-12

### Added
- Slack API設定変更の検知機能を追加（トークン・チャンネル設定変更時に自動で再接続）

### Changed
- トークン設定UIの改善とデバッグログの強化
- 依存パッケージのバージョンアップ

---

## [1.3.1] - 2026-03-02

### Added
- release skill を追加（リリース作業の自動化）

### Changed
- `out/` を .gitignore に追加

---

## [1.3.0] - 2026-02-27

### Added
- 管理ウィンドウへのログ表示エリアを追加し、Slack診断ログを整備

### Fixed
- カスタム絵文字のレースコンディションを修正

### Changed
- チャンネル一覧で参加チャンネルを上位に表示し、未参加チャンネルを選択不可に
- ハードコードの絵文字マップを gemoji パッケージに置き換え
- `window.electronAPI` 互換レイヤーを削除し、`tauriAPI` の直接 import に移行
- Tailwind CSS v3 → v4 にメジャーアップデート
- Vite v5 → v7、`@vitejs/plugin-react` v4 → v5 にメジャーアップデート
- postcss を直接依存から削除し、Cargo.lock のバージョンを同期
- 依存パッケージのマイナー/パッチアップデート

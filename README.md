# Leaner Waigaya

Slack の特定のチャンネルの会話をデスクトップに透過表示させて、チームの「わいわいがやがや」した雰囲気を感じられるようにするデスクトップアクセサリです。

## 主な機能

- 指定した Slack チャンネルのメッセージをリアルタイムで表示
- デスクトップ上に常に最前面で透過表示
- 複数のウィンドウ、複数のチャンネルに対応
- カスタム絵文字の表示

## ダウンロードと実行方法

### 1. アプリケーションのダウンロード

1.  リポジトリの[Actions](https://github.com/leaner-co-jp/leaner-waigaya/actions)タブに移動します。
2.  左のサイドバーから「Build & Release」ワークフローを選択します。
3.  最新の実行結果（緑色のチェックマークが付いているもの）をクリックします。
4.  ページ下部の「Artifacts」セクションから、お使いの OS に合ったファイルをダウンロードします。
    - macOS: `leaner-waigaya-macos-latest`
    - Windows: `leaner-waigaya-windows-latest`

### 2. macOS での実行方法

macOS のセキュリティ機能（Gatekeeper）により、ダウンロードしたアプリケーションをそのままでは開けない場合があります。その場合は、以下の手順で実行してください。

1.  ダウンロードした zip ファイルを解凍します。
2.  ターミナルを開き、`cd`コマンドでアプリケーションが保存されているディレクトリに移動します。
    ```sh
    # 例：ダウンロードフォルダに保存した場合
    cd ~/Downloads/
    ```
3.  以下のコマンドを実行して、macOS の検疫属性を解除します。
    ```sh
    xattr -d com.apple.quarantine "Leaner Waigaya.app"
    ```
4.  これで、アプリケーションをダブルクリックで起動できるようになります。

### 3. Windows での実行方法

Windows のセキュリティ機能（SmartScreen）により、インストーラーの実行時に警告が表示されることがあります。

1.  ダウンロードした zip ファイルを解凍します。
2.  解凍したフォルダの中にある `Setup.exe` (または `leaner-waigaya.exe`) を実行してインストールします。
3.  「Windows によって PC が保護されました」という画面が表示された場合は、「詳細情報」をクリックします。
4.  次に表示される「実行」ボタンをクリックすると、インストールが開始されます。
5.  インストール完了後、アプリケーションが自動的に起動し、デスクトップやスタートメニューにショートカットが作成されます。

---

This project was built with Electron, React, TypeScript, and Vite.
